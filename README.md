# 🧠 Java Quiz Program

This is a simple **console-based quiz program** written in **Java**. It presents multiple-choice questions to the user, takes their input, and shows the final score at the end.

## ✅ Features

- 5 Multiple-choice questions
- Each question has 4 options
- User selects the correct option using console input
- Score is calculated and displayed at the end

## 🛠️ Technologies Used

- Java (Core)
- No external libraries
- Runs on command line / terminal

## 📁 Project Structure

The project contains **3 Java files**:

1. `Question.java`  
   Contains the blueprint for a question (id, question text, options, and answer)

2. `QuestionService.java`  
   Initializes questions and stores user selections

3. `Main.java` (or `QuizApp.java`)  
   The main driver class that runs the quiz

## 🔧 How to Run

1. Clone or download the repository
2. Open terminal or any Java IDE
3. Compile the files:
   ```bash
   javac *.java
